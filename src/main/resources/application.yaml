spring:
  application:
    name: yunplant
  datasource:
    username: root
    password: 122300
    url: jdbc:mysql://localhost:3306/yunplant?characterEncoding=utf8&useUnicode=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
server:
  port: 8080
mybatis:
  mapper-locations: classpath:mapper/*.xml